import type { Metadata, Viewport } from 'next';
import { Inter } from 'next/font/google';
import './globals.css';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'Podcast Summarizer - AI-Powered Episode Summaries',
  description: 'Discover and summarize podcast episodes using AI technology. Get instant summaries of your favorite podcasts.',
  keywords: 'podcast, summarizer, AI, episodes, technology',
  authors: [{ name: 'Podcast Summarizer Team' }],
};

export const viewport: Viewport = {
  width: 'device-width',
  initialScale: 1,
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <div className="min-h-screen bg-gray-50">
          {children}
        </div>
      </body>
    </html>
  );
} 